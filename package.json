{
  "name": "ait-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "packages/*",
    "examples/*",
    "templates/*"
  ],
  "scripts": {
    "typecheck:packages": "bun --filter '@ait-kit/*' typecheck",
    "typecheck:apps": "bun run packages:build && cd templates/cloudflare-toss-api-service && bun run typecheck && cd ../../examples/cloudflare-pages-gateway-advanced && bun run typecheck",
    "typecheck": "bun run typecheck:packages && bun run typecheck:apps",
    "test": "bun --filter '@ait-kit/*' test",
    "test:packages": "node scripts/test-package-tarballs.mjs",
    "packages:build": "node scripts/build-packages.mjs",
    "build": "bun run packages:build && cd examples/cloudflare-pages-gateway-advanced && bun run build",
    "deploy:dry-run": "cd packages/api-cloudflare-service && bun run deploy:dry-run && cd ../../templates/cloudflare-toss-api-service && bun run deploy:dry-run",
    "check:internal-deps": "node scripts/sync-internal-dependencies.mjs --check",
    "sync:internal-deps": "node scripts/sync-internal-dependencies.mjs",
    "check": "bun run check:internal-deps && bun run typecheck && bun run test && bun run build && bun run test:packages && bun run deploy:dry-run",
    "release": "sampo release",
    "publish:oidc": "bash scripts/publish-oidc.sh",
    "publish:dry-run": "bun run packages:build && bun run test:packages && DRY_RUN=1 bash scripts/publish-oidc.sh",
    "sampo:add": "sampo add"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260712.1",
    "@types/bun": "1.3.14",
    "typescript": "6.0.3",
    "wrangler": "4.110.0"
  }
}
