---
npm/@ait-kit/sdk: minor
---

Add React Native and Web review adapters with lazy SDK loading, per-call support checks, and shared in-flight requests. Completion only reports SDK call completion, never review submission or reward eligibility. Optional official SDK peer minimums remain unchanged.
